{
  "name": "server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "刷题后端：Express + PostgreSQL (Supabase)",
  "scripts": {
    "dev": "node --env-file=.env --watch src/index.js",
    "start": "node --env-file=.env src/index.js",
    "seed": "node --env-file=.env scripts/seed.mjs",
    "postgrest": "node --env-file=.env scripts/postgrest.mjs",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.3.7",
    "eslint": "^9.39.5",
    "eslint-config-google": "^0.14.0",
    "globals": "^17.12.0"
  }
}
